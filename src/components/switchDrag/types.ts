export type SwitchDragType = {
  isChecked: boolean;
  onSwitchChange: () => void;
};

export type SwitchDragComponentType = {
  isChecked: boolean;
  isFilterAll: boolean;
  onSwitchChange: () => void;
};
